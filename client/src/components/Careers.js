import React from "react";
import Header from "./Header";

export const Careers = () => {
  return (
    <Header activeItem="careers">
      <h2 className="header">Careers page</h2>
    </Header>
  );
};

export default Careers;
